// swift-tools-version:5.5

import PackageDescription

let package = Package(
    name: "cutbox",
    dependencies: [],
    targets: [
        .executableTarget(
            name: "cutbox",
            dependencies: []),
    ]
)
