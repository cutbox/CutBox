name: CutBox Build

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build:
    name: CutBox Build
    runs-on: macos-11

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup
        run: |
          
          bin/setup

      - name: Build
        run: |
          bin/build
